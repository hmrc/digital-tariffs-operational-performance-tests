services {
  tariff-classification-frontend {
    protocol = https
    host = admin.staging.tax.service.gov.uk
    port = 443
  }
    stride-idp-stub {
      protocol = https
      host = admin.staging.tax.service.gov.uk
      port = 443
    }
}
